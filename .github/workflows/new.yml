name: Hello Workflow

on:
  push:
    branches: ["main"]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Print Message
        run: echo "Hello Harshada, your GitHub Action YAML is working!"
